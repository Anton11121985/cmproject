<collection name="ScientificWorks_(vw_cmj_all)" idField="id" replace="runtime">
    <prototype>
        <![CDATA[Select
	id,
	created_date,
	updated_date,
	Module,
	self_1,
	self_2,
	self_3,
	SubjectDoc,
	Tema,
	subject,
	Field,
	Author,
	Coauthor,
	Registrationdate,
	Materials
from
	(
	select
		rkkbase.id,
		rkkbase.created_date,
		rkkbase.updated_date,
		rkkbase.module as module,
		rkkbase.subject as subject,
		'<id>' as self_1,
		':' as self_2,
		'</>' as self_3,
		(
		select
			tfs."value" as v
		from
			tn_field
		join tn_field_string tfs on
			tn_field.access_object_id = tfs.id
		where
			tn_field."owner" = rkkbase.id
			and tn_field.owner_type = rkkbase.id_type
			and tn_field.cmjfield = 'SubjectDoc') as SubjectDoc,
		(select
			tfs."value" as v
		from
			tn_field
		join tn_field_string tfs on
			tn_field.access_object_id = tfs.id
		where
			tn_field."owner" = rkkbase.id
			and tn_field.owner_type = rkkbase.id_type
			and tn_field.cmjfield = 'Tema') as Tema,
		(select
			tfs."value" as v
		from
			tn_field
		join tn_field_string tfs on
			tn_field.access_object_id = tfs.id
		where
			tn_field."owner" = rkkbase.id
			and tn_field.owner_type = rkkbase.id_type
			and tn_field.cmjfield = 'Field') as Field,
		(select
			string_agg(SPLIT_PART(tfs.value, '%', 1),',') as v
		from
			tn_field
		join tn_field_string tfs on
			tn_field.access_object_id = tfs.id
		where
			tn_field."owner" = rkkbase.id
			and tn_field.owner_type = rkkbase.id_type
			and tn_field.cmjfield = 'Author') as Author,
		(select
			string_agg(SPLIT_PART(tfs.value, '%', 1),',') as v
		from
			tn_field
		join tn_field_string tfs on
			tn_field.access_object_id = tfs.id
		where
			tn_field."owner" = rkkbase.id
			and tn_field.owner_type = rkkbase.id_type
			and tn_field.cmjfield = 'Coauthor') as Coauthor,
		(select
			tfs."value" as v
		from
			tn_field
		join tn_field_string tfs on
			tn_field.access_object_id = tfs.id
		where
			tn_field."owner" = rkkbase.id
			and tn_field.owner_type = rkkbase.id_type
			and tn_field.cmjfield = 'Registrationdate') as Registrationdate,
		(select
			tfs."value" as v
		from
			tn_field
		join tn_field_string tfs on
			tn_field.access_object_id = tfs.id
		where
			tn_field."owner" = rkkbase.id
			and tn_field.owner_type = rkkbase.id_type
			and tn_field.cmjfield = 'Materials') as Materials
	from
		F_DP_Intrkk internalrkk
	join f_dp_rkk rkk on
		rkk.id = internalrkk.id
	join f_dp_rkkbase rkkbase on
		rkkbase.id = rkk.id
	where
		rkkbase.isdeleted <> 1) s
where 1=1  ::where-clause]]>
    </prototype>
    <counting-prototype>
        <![CDATA[
            SELECT
				COUNT(1)
                FROM (
                       select
	rkkbase.id,
    rkkbase.created_date,
    rkkbase.updated_date,
    rkkbase.module                                   AS Module,
    '<id>'                                           AS self_1,
    ':'                                              AS self_2,
    '</>'                                            AS self_3
	FROM F_DP_Intrkk internalrkk
                  JOIN f_dp_rkk rkk ON rkk.id = internalrkk.id
                  JOIN f_dp_rkkbase rkkbase ON rkkbase.id = rkk.id
                  JOIN F_DP_Intrkk_Executor executor ON executor.owner = internalrkk.id
                  JOIN so_beard beard ON beard.id = executor.executor
         WHERE rkkbase.isdeleted <> 1
           AND rkk.regnumcnt IS NULL
		   ) s
                WHERE 1=1 ::where-clause
            ]]>
    </counting-prototype>
    <filter name="MODULE">
        <criteria placeholder="where-clause">
            <![CDATA[
                    Module = {0}
                ]]>
        </criteria>
    </filter>
    <filter name="self">
        <criteria placeholder="where-clause"><![CDATA[ Module = {0} and id = {1} ]]></criteria>
    </filter>
    <filter name="created_date">
        <criteria placeholder="where-clause">
            <![CDATA[
                   cast(created_date as date) = cast({0} as date)
                ]]>
        </criteria>
    </filter>
    <filter name="SubjectDoc">
        <criteria placeholder="where-clause">
            <![CDATA[
                   SubjectDoc = {0}
                ]]>
        </criteria>
    </filter>
    <filter name="Tema">
        <criteria placeholder="where-clause">
        <![CDATA[
                   Tema = {0}
                ]]>
        </criteria>
    </filter>
    <filter name="Coauthor">
        <criteria placeholder="where-clause">
            <![CDATA[
                   Coauthor = {0}
                ]]>
        </criteria>
    </filter>
    <filter name="Registrationdate">
        <criteria placeholder="where-clause">
            <![CDATA[
                   Registrationdate = {0}
                ]]>
        </criteria>
    </filter>
    <filter name="Materials">
        <criteria placeholder="where-clause">
            <![CDATA[
                   Materials = {0}
                ]]>
        </criteria>
    </filter>
</collection>