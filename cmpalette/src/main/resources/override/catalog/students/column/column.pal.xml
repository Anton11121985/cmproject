<?xml version='1.0' encoding='UTF-8'?>
<catalog xmlns="http://www.intertrust.ru/schema/palette/cat-ui">
  <column name="nameFull" extends="string" subject="${nameFull:ФИО студента}"/>
  <column name="birthDate" extends="date" subject="${birthDate:Дата рождения}"/>
  <column name="startDate" extends="date" subject="${startDate:Дата поступления}"/>
  <column name="group_st" extends="string" subject="${group:Группа}"/>
  <column name="course" extends="string" subject="${course:Курс}"/>
  <column name="speciality" extends="string" subject="${speciality:Специальность}"/>
  <column name="docNum" extends="string" subject="${nameFull:Номер документа}"/>
  <column name="speciality" extends="string" subject="${startDate:Специальность}"/>
  <column name="docDate" extends="date" subject="${course:Дата документа}"/>
</catalog>