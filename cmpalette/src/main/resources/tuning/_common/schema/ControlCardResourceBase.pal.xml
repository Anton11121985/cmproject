<?xml version='1.0' encoding='UTF-8'?>
<schemas xmlns="http://www.intertrust.ru/schema/palette/tn-schema">
    <schema name="ControlCardResourceBase" recalc="true">
        <meta>
            <category name="Сервис\Контроль"/>
        </meta>
        <!-- Общие системные поля. Begin -->
        <boolean name="isSupervisor" system="true"/>
        <boolean name="isControlled_onDisk" system="true"/>
        <boolean name="control_readonly" system="true"/>
        <boolean name="deadline_readonly" system="true"/>
        <boolean name="changedInReadMode" system="true"/>
        <boolean name="executed" system="true"/>
        <boolean name="isControlled" system="true"/>
        <boolean name="controllerIsEmpty" system="true"/>
        <actor name="controller" array="true" system="true"/>
        <date name="deadline" system="true" createlog="true"/>
        <!-- Общие системные поля. End -->
        <!-- Общие системные темповые поля. Begin -->
        <boolean name="isControlled_tmp" system="true"/>
        <actor name="controller_tmp" array="true" system="true"/>
        <date name="deadline_tmp" system="true"/>
        <!-- Общие системные темповые поля. End -->
    </schema>
</schemas>